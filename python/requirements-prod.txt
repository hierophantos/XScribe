# XScribe Production Dependencies
# Optimized for minimal bundle size (~700MB target)
#
# Key optimizations:
# - CPU-only PyTorch (saves ~140MB vs CUDA)
# - No pyinstaller (build-time only)
# - Removed unused indirect dependencies

# CPU-only PyTorch (must be specified first)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0,<2.6.0
torchaudio>=2.0.0,<2.6.0

# WhisperX - transcription with word-level timestamps
# Must be installed from GitHub (not on PyPI)
# Note: WhisperX requires pyannote.audio 3.x (not 4.x) for compatibility
git+https://github.com/m-bain/whisperX.git
pyannote.audio>=3.1,<4.0

# Sherpa-ONNX for token-free speaker diarization
# Uses ONNX models - no HuggingFace token required
sherpa-onnx>=1.10.0

# Audio processing
soundfile
av
